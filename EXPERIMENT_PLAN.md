# 의미 기반 검색 POC 실험 계획서

## 1. 프로젝트 개요

### 1.1 목적

사람들의 일기 데이터를 대상으로 다양한 의미 기반 검색(Semantic Search) 기술을 적용하여, 각 기술의 특성과 한계를 실험적으로 검증한다. 단순 벡터 검색부터 Graph RAG까지 단계적으로 기술 수준을 높여가며, 어떤 기능까지 구현 가능한지 확인한다.

### 1.2 시나리오

- **5명의 가상 인물**이 존재하며, 각 인물은 서로 다른 성격/직업/관계를 가짐
- 각 인물은 **6개월~1년 분량의 일기**를 보유 (인물당 약 100~200개 항목)
- 일기에는 감정, 사건, 인물 간 관계, 장소, 시간 정보가 자연스럽게 포함됨
- 일기 간 **교차 관계** 존재 (같은 이벤트를 다른 시각으로 기록)

### 1.3 핵심 질문

> "벡터 검색, 하이브리드 검색, Graph RAG 각각이 일기 데이터에서 어떤 수준의 검색/분석 기능을 제공할 수 있는가?"

---

## 2. 합성 데이터 설계

### 2.1 인물 프로필

| 인물 | 이름 | 직업 | 성격 | 관계 |
|------|------|------|------|------|
| A | 김민준 | 소프트웨어 개발자 | 내향적, 분석적 | B의 대학 동기, D의 연인 |
| B | 이서연 | 프리랜서 디자이너 | 외향적, 감성적 | A의 대학 동기, C의 직장 동료 |
| C | 박지호 | 카페 사장 | 낙관적, 사교적 | B의 직장 동료(카페 디자인), E의 이웃 |
| D | 최수아 | 대학원생 (심리학) | 신중한, 관찰력 있는 | A의 연인, E의 스터디 멤버 |
| E | 정우진 | 고등학교 교사 | 책임감 강한, 따뜻한 | C의 이웃, D의 스터디 멤버 |

### 2.2 데이터 구조

```json
{
  "entry_id": "diary_A_2025-03-15",
  "person_id": "A",
  "person_name": "김민준",
  "date": "2025-03-15",
  "day_of_week": "토요일",
  "weather": "맑음",
  "mood": "불안",
  "mood_score": 3,
  "tags": ["직장", "발표", "스트레스"],
  "title": "월요일 발표가 걱정된다",
  "content": "주말인데도 월요일 발표 생각에 마음이 편하지 않다. 서연이한테 연락해서 슬라이드 디자인 봐달라고 할까 고민 중이다...",
  "mentioned_people": ["이서연"],
  "mentioned_places": ["회사", "집"],
  "mentioned_events": ["월요일 발표"]
}
```

### 2.3 데이터 생성 전략

#### 의도적으로 포함할 패턴

| 패턴 | 목적 | 예시 |
|------|------|------|
| **동의어/유사 표현** | 벡터 검색의 의미 매칭 테스트 | "불안하다" vs "초조하다" vs "걱정된다" vs "마음이 무겁다" |
| **교차 이벤트** | 다중 시점 검색 테스트 | A와 B가 같은 모임을 각자의 일기에 기록 |
| **시간 흐름에 따른 감정 변화** | 시계열 + 의미 검색 테스트 | A의 발표 준비 → 발표 당일 → 발표 후기 |
| **암시적 관계** | Graph RAG 관계 추출 테스트 | "서연이 소개해준 카페가 좋았다" → B-C 관계 추론 |
| **반복 주제** | 테마 추출 테스트 | C의 일기에 반복적으로 등장하는 "카페 매출 걱정" |
| **특정 키워드 없는 의미** | 키워드 검색 vs 벡터 검색 비교 | "마음이 공허하다" → "우울" 관련 검색에 매칭되어야 함 |
| **다국어/신조어** | 임베딩 모델 견고성 테스트 | "오늘 너무 TMI했다", "워라밸 최악" |

#### 데이터 생성 규모

| 항목 | 수량 |
|------|------|
| 인물 수 | 5명 |
| 인물당 일기 | 150개 (약 6개월) |
| 총 일기 수 | 750개 |
| 일기당 평균 길이 | 200~500자 |
| 교차 이벤트 | 약 30건 (인물 간 공유 이벤트) |
| 고유 장소 | 약 25개 |
| 고유 이벤트 | 약 50개 |

#### 생성 방법

- **LLM(GPT-4o / Claude) 활용**: 인물 프로필과 시나리오를 프롬프트로 제공하여 자연스러운 일기 생성
- **후처리**: 메타데이터(mood, tags, mentioned_people 등) 자동 추출 및 수동 검수
- **시드 이벤트 목록**: 사전에 정의한 30개의 공유 이벤트를 기반으로 교차 참조 보장

---

## 3. 실험 기술 스택

### 3.1 기술 개요도

```
┌─────────────────────────────────────────────────────────┐
│                    실험 기술 스택                          │
├───────────┬───────────┬───────────┬─────────────────────┤
│  Phase 1  │  Phase 2  │  Phase 3  │      Phase 4        │
│  벡터검색  │ 하이브리드 │ Graph RAG │  통합 파이프라인      │
├───────────┼───────────┼───────────┼─────────────────────┤
│ ChromaDB  │ +BM25     │ GraphRAG  │ Vector+BM25+Graph   │
│ FAISS     │ +Re-rank  │ LightRAG  │ +Re-ranker          │
│           │           │ Neo4j     │ +LLM 답변 생성       │
├───────────┴───────────┴───────────┴─────────────────────┤
│              Embedding Models Layer                      │
│  all-MiniLM-L6-v2 │ BGE-M3 │ text-embedding-3-small    │
├─────────────────────────────────────────────────────────┤
│              Python + LangChain / LlamaIndex             │
└─────────────────────────────────────────────────────────┘
```

### 3.2 임베딩 모델 비교

| 모델 | 차원 | 비용 | 다국어 | 특징 |
|------|------|------|--------|------|
| `all-MiniLM-L6-v2` | 384 | 무료(로컬) | 제한적 | 빠르고 가벼움, 프로토타입 적합 |
| `all-mpnet-base-v2` | 768 | 무료(로컬) | 제한적 | MiniLM보다 높은 품질 |
| `BGE-M3` | 1024 | 무료(로컬) | 100+언어 | 한국어 지원 우수, MIT 라이선스 |
| `text-embedding-3-small` | 1536 | $0.02/1M토큰 | 우수 | OpenAI API, 높은 품질 |
| `text-embedding-3-large` | 3072 | $0.13/1M토큰 | 우수 | 최고 품질, 차원 조절 가능 |

### 3.3 벡터 DB 비교

| DB | 타입 | 메타데이터 필터링 | 하이브리드 검색 | 적합 단계 |
|----|------|-----------------|---------------|----------|
| **ChromaDB** | 임베디드(로컬) | O | X | Phase 1 |
| **FAISS** | 라이브러리 | X | X | Phase 1 (벤치마크) |
| **Qdrant** | 서버/임베디드 | O (강력) | O (sparse+dense) | Phase 2~4 |
| **Weaviate** | 서버 | O | O (BM25+벡터) | Phase 2~4 |

### 3.4 Graph RAG 기술 비교

| 기술 | 특징 | 비용 | 복잡도 | 적합 용도 |
|------|------|------|--------|----------|
| **Microsoft GraphRAG** | 커뮤니티 탐지, 글로벌/로컬 검색 | 높음 (LLM 인덱싱) | 높음 | 테마 분석, 전체 요약 |
| **LightRAG** | 경량, 빠른 업데이트 | 낮음 | 중간 | 실시간 엔티티 관계 검색 |
| **Neo4j GraphRAG** | 완전한 그래프 DB, Cypher 쿼리 | 중간 | 높음 | 복잡한 관계 탐색 |
| **nano-graphrag** | 초경량, 학습용 | 낮음 | 낮음 | 빠른 실험/프로토타입 |

---

## 4. 실험 단계별 계획

### Phase 1: 순수 벡터 검색 (Baseline)

#### 목표
벡터 임베딩만으로 일기 데이터에서 의미 기반 검색이 얼마나 효과적인지 측정한다.

#### 실험 구성

| 실험 ID | 임베딩 모델 | 벡터 DB | 청킹 전략 |
|---------|------------|---------|----------|
| V-1 | all-MiniLM-L6-v2 | ChromaDB | 일기 1개 = 1청크 |
| V-2 | BGE-M3 | ChromaDB | 일기 1개 = 1청크 |
| V-3 | text-embedding-3-small | ChromaDB | 일기 1개 = 1청크 |
| V-4 | all-MiniLM-L6-v2 | FAISS | 일기 1개 = 1청크 |
| V-5 | BGE-M3 | ChromaDB | 문단 단위 청킹 |

#### 테스트 쿼리 셋

```
카테고리 1: 감정 기반 검색
  Q1: "스트레스 받는 날들"
  Q2: "행복했던 순간"
  Q3: "외로움을 느꼈던 때"
  Q4: "마음이 편안했던 날" (← "평화롭다", "여유롭다" 등 동의어 매칭 테스트)

카테고리 2: 이벤트 기반 검색
  Q5: "친구들과 만난 날"
  Q6: "직장에서 문제가 있었던 날"
  Q7: "여행 관련 이야기"
  Q8: "새로운 것을 시작한 경험"

카테고리 3: 사람 기반 검색
  Q9: "서연이와 관련된 이야기"
  Q10: "연인과 함께한 시간"

카테고리 4: 추상적 질문
  Q11: "인생에 대한 고민"
  Q12: "미래에 대한 계획"
  Q13: "자기 반성"

카테고리 5: 시간 + 의미 복합
  Q14: "봄에 기분이 좋았던 날"
  Q15: "비 오는 날의 감정"
```

#### 측정 지표

| 지표 | 설명 |
|------|------|
| **Precision@K** (K=5,10) | 상위 K개 결과 중 관련 있는 비율 |
| **Recall@K** (K=10,20) | 전체 관련 문서 중 상위 K개에 포함된 비율 |
| **MRR** (Mean Reciprocal Rank) | 첫 번째 관련 결과의 순위 역수 평균 |
| **Latency** | 쿼리당 응답 시간 (ms) |
| **주관적 관련성** | 1~5점 수동 평가 |

#### 구현 가능 기능

- [x] 유사한 감정의 일기 검색
- [x] 특정 이벤트/주제 관련 일기 찾기
- [x] "이런 느낌의 글" 탐색
- [ ] 정확한 날짜 기반 검색 (메타데이터 필터 필요)
- [ ] 사람 간 관계 파악
- [ ] 시간에 따른 감정 변화 추적

---

### Phase 2: 하이브리드 검색 (Vector + BM25 + 메타데이터)

#### 목표
벡터 검색에 키워드 검색(BM25)과 메타데이터 필터링을 결합하여 검색 품질 향상을 측정한다.

#### 실험 구성

| 실험 ID | 구성 | 융합 방법 | Re-ranker |
|---------|------|----------|-----------|
| H-1 | BGE-M3 + BM25 | RRF (k=60) | 없음 |
| H-2 | BGE-M3 + BM25 | Weighted (α=0.7) | 없음 |
| H-3 | BGE-M3 + BM25 | RRF (k=60) | cross-encoder/ms-marco-MiniLM-L-6-v2 |
| H-4 | BGE-M3 + BM25 + 메타데이터 | RRF | cross-encoder |
| H-5 | Qdrant sparse+dense | 네이티브 하이브리드 | 없음 |
| H-6 | Weaviate hybrid | 네이티브 하이브리드 (α=0.5) | 없음 |

#### 메타데이터 필터 활용

```python
# 시간 범위 필터
results = search(query="스트레스", filter={"date": {"$gte": "2025-03-01", "$lte": "2025-03-31"}})

# 인물 필터
results = search(query="재미있었던 날", filter={"person_id": "A"})

# 감정 필터
results = search(query="일상", filter={"mood_score": {"$lte": 2}})  # 기분 나빴던 날만

# 복합 필터
results = search(
    query="직장 이야기",
    filter={"$and": [{"person_id": "A"}, {"tags": {"$contains": "직장"}}]}
)
```

#### Phase 1 대비 추가 구현 가능 기능

- [x] 정확한 이름/장소 키워드 매칭 + 의미 검색 결합
- [x] 날짜 범위 지정 검색
- [x] 감정 점수 기반 필터링
- [x] 특정 인물의 일기만 검색
- [x] 태그 기반 필터링
- [ ] 사람 간 관계 파악 (여전히 불가)
- [ ] "A가 B를 어떻게 생각하는지" 같은 관계 질문

---

### Phase 3: Graph RAG

#### 목표
지식 그래프를 구축하여 엔티티 간 관계를 파악하고, 벡터 검색만으로는 불가능한 복합 질문에 답변한다.

#### 실험 3-A: Microsoft GraphRAG

##### 구성
```
일기 데이터 (750개)
    ↓ LLM 기반 엔티티/관계 추출
지식 그래프 구축
    ↓ 커뮤니티 탐지 (Leiden algorithm)
커뮤니티 계층 구조 + 요약 생성
    ↓
Global Search / Local Search
```

##### 테스트 쿼리

```
글로벌 검색 (테마/패턴 분석):
  GQ1: "모든 인물들의 일기에서 공통적으로 나타나는 주제는?"
  GQ2: "6개월간 전체적인 감정 흐름은 어떤가?"
  GQ3: "인물들 사이의 관계 네트워크를 요약해줘"

로컬 검색 (특정 엔티티 중심):
  LQ1: "김민준과 이서연의 관계는 시간에 따라 어떻게 변했나?"
  LQ2: "박지호의 카페와 관련된 모든 사건을 정리해줘"
  LQ3: "정우진이 언급한 학생들에 대한 이야기를 모아줘"
```

#### 실험 3-B: LightRAG

##### 구성
```
일기 데이터 (750개)
    ↓ LLM 기반 엔티티/관계 추출 (경량)
듀얼 레벨 검색 인덱스 구축
    ↓
Low-level (엔티티 검색) / High-level (테마 검색)
```

##### 테스트 쿼리

```
Low-level:
  LL1: "김민준의 직장 동료들은 누구인가?"
  LL2: "이서연이 방문한 장소들은?"

High-level:
  HL1: "인물들의 주요 고민거리 패턴은?"
  HL2: "인간관계에서 반복되는 갈등 유형은?"
```

#### 실험 3-C: Neo4j + GraphRAG

##### 그래프 스키마 설계

```
노드 타입:
  - Person (이름, 직업, 성격)
  - DiaryEntry (날짜, 감정, 내용 요약)
  - Event (이름, 날짜, 설명)
  - Place (이름, 타입)
  - Theme (이름: 직장스트레스, 연애, 건강 등)
  - Emotion (이름, 강도)

관계 타입:
  - WROTE (Person → DiaryEntry)
  - MENTIONS (DiaryEntry → Person/Place/Event)
  - PARTICIPATED_IN (Person → Event)
  - FEELS (DiaryEntry → Emotion)
  - RELATES_TO (Person → Person, type: 친구/연인/동료)
  - HAS_THEME (DiaryEntry → Theme)
  - LOCATED_AT (Event → Place)
```

##### Cypher 쿼리 예시

```cypher
// 두 사람 사이의 관계 경로 탐색
MATCH path = shortestPath(
  (a:Person {name: "김민준"})-[*]-(b:Person {name: "박지호"})
)
RETURN path

// 특정 감정을 공유한 날 찾기
MATCH (p1:Person)-[:WROTE]->(d1:DiaryEntry)-[:FEELS]->(e:Emotion {name: "불안"}),
      (p2:Person)-[:WROTE]->(d2:DiaryEntry)-[:FEELS]->(e)
WHERE p1 <> p2 AND d1.date = d2.date
RETURN p1.name, p2.name, d1.date

// 인물 중심성 분석 (누가 가장 많이 언급되는가)
MATCH (p:Person)<-[:MENTIONS]-(d:DiaryEntry)
RETURN p.name, count(d) AS mention_count
ORDER BY mention_count DESC
```

#### Phase 2 대비 추가 구현 가능 기능

- [x] "A와 B의 관계가 시간에 따라 어떻게 변했는가?"
- [x] "모든 인물에게 공통된 주제/고민은?"
- [x] "이 사건에 관련된 모든 사람과 그들의 시각은?"
- [x] 인물 관계 네트워크 시각화
- [x] 감정 흐름 타임라인 생성
- [x] 커뮤니티/그룹 자동 탐지
- [x] "왜 A가 이런 감정을 느꼈을까?" 같은 인과 추론 (LLM 결합 시)

---

### Phase 4: 통합 파이프라인

#### 목표
모든 기술을 통합한 최종 파이프라인을 구축하고, 쿼리 유형에 따라 최적의 검색 전략을 자동 선택한다.

#### 아키텍처

```
사용자 질문
    ↓
┌──────────────────┐
│  Query Classifier │ ← LLM 기반 쿼리 분류
│  (질문 유형 분석)  │
└──────┬───────────┘
       ↓
┌──────────────────────────────────────┐
│         Router (라우터)               │
├──────────┬───────────┬───────────────┤
│ 단순 검색 │ 복합 검색  │ 관계/분석 질문  │
│          │           │               │
│ Vector   │ Hybrid    │ Graph RAG     │
│ Search   │ Search    │ + Vector      │
│          │ + Filter  │               │
├──────────┴───────────┴───────────────┤
│         Result Merger                 │
│    (결과 병합 + Re-ranking)           │
├──────────────────────────────────────┤
│         LLM Answer Generator          │
│    (검색 결과 기반 자연어 답변 생성)     │
└──────────────────────────────────────┘
```

#### 쿼리 라우팅 규칙

| 질문 유형 | 라우팅 대상 | 예시 |
|----------|-----------|------|
| 감정/의미 기반 | Vector Search | "행복했던 날" |
| 키워드 + 의미 | Hybrid Search | "카페에서 기분 좋았던 날" |
| 시간+조건 결합 | Hybrid + Filter | "3월에 스트레스 받은 날" |
| 관계 질문 | Graph RAG | "민준이와 서연이 관계" |
| 테마/패턴 분석 | GraphRAG Global | "공통적인 고민" |
| 인과/추론 | Graph + Vector + LLM | "왜 수아가 우울했을까" |

#### 통합 테스트 쿼리 (최종)

```
복합 질문 (모든 기술 동원):
  FQ1: "김민준이 가장 힘들어했던 시기는 언제이고, 그때 주변 사람들은 어떤 상황이었나?"
  FQ2: "이서연과 박지호가 함께 작업한 프로젝트의 진행 과정을 시간순으로 정리해줘"
  FQ3: "모든 인물 중 가장 감정 기복이 심한 사람은 누구이고, 그 원인은 뭘까?"
  FQ4: "최수아의 논문 스트레스가 김민준과의 관계에 미친 영향은?"
  FQ5: "정우진이 가장 보람을 느꼈던 순간들의 공통점은?"
  FQ6: "비 오는 날 인물들의 감정 패턴에 차이가 있는가?"
  FQ7: "5명의 인물 중 서로 만나지 않는 조합은? 그리고 간접적으로는 어떻게 연결되어 있는가?"
```

---

## 5. 평가 프레임워크

### 5.1 정량적 평가

| 지표 | Phase 1 | Phase 2 | Phase 3 | Phase 4 |
|------|---------|---------|---------|---------|
| Precision@5 | 측정 | 측정 | 측정 | 측정 |
| Recall@10 | 측정 | 측정 | 측정 | 측정 |
| MRR | 측정 | 측정 | 측정 | 측정 |
| Latency (ms) | 측정 | 측정 | 측정 | 측정 |
| 인덱싱 시간 | 측정 | 측정 | 측정 | 측정 |
| 스토리지 크기 | 측정 | 측정 | 측정 | 측정 |

### 5.2 정성적 평가

| 평가 항목 | 설명 | 점수 (1~5) |
|----------|------|-----------|
| 의미 관련성 | 검색 결과가 의미적으로 관련 있는가 | |
| 답변 완전성 | 질문에 대한 답변이 완전한가 | |
| 관계 파악 정확도 | 인물/사건 간 관계를 정확히 파악하는가 | |
| 시간적 정확성 | 시간 순서와 흐름을 잘 반영하는가 | |
| 추론 능력 | 명시적으로 작성되지 않은 것을 추론하는가 | |

### 5.3 Ground Truth 구성

데이터 생성 시 각 테스트 쿼리에 대한 **정답 셋(ground truth)**을 함께 생성한다.

```json
{
  "query_id": "Q1",
  "query": "스트레스 받는 날들",
  "relevant_entries": ["diary_A_2025-03-15", "diary_A_2025-04-02", "diary_D_2025-03-20", ...],
  "relevance_scores": {"diary_A_2025-03-15": 5, "diary_A_2025-04-02": 4, ...},
  "expected_answer_keywords": ["발표", "마감", "논문", "야근"]
}
```

---

## 6. 기술별 구현 가능 기능 매트릭스

| 기능 | Vector Only | Hybrid | Graph RAG | 통합 |
|------|:-----------:|:------:|:---------:|:----:|
| 유사 감정 일기 검색 | ● | ● | ● | ● |
| 동의어/유사표현 매칭 | ● | ● | ● | ● |
| 정확한 이름 검색 | △ | ● | ● | ● |
| 날짜 범위 필터링 | ○ | ● | ● | ● |
| 태그/카테고리 필터 | ○ | ● | ● | ● |
| 인물 간 관계 파악 | ✗ | ✗ | ● | ● |
| 시간에 따른 감정 변화 | ✗ | △ | ● | ● |
| 교차 시점 이벤트 추적 | ✗ | △ | ● | ● |
| 테마/패턴 자동 분석 | ✗ | ✗ | ● | ● |
| 인과 관계 추론 | ✗ | ✗ | △ | ● |
| 커뮤니티/그룹 탐지 | ✗ | ✗ | ● | ● |
| 자연어 답변 생성 | ✗ | ✗ | ● | ● |
| 복합 조건 질문 응답 | ✗ | △ | △ | ● |
| 관계 네트워크 시각화 | ✗ | ✗ | ● | ● |
| 질문 유형 자동 라우팅 | ✗ | ✗ | ✗ | ● |

> ● 완전 지원 | △ 부분 지원 | ○ 메타데이터 활용 시 가능 | ✗ 불가

---

## 7. 기술 스택 요약

### 필수 패키지

```bash
# 기본 환경
pip install python-dotenv pandas numpy matplotlib seaborn

# Phase 1: 벡터 검색
pip install chromadb sentence-transformers faiss-cpu

# Phase 2: 하이브리드 검색
pip install rank-bm25 qdrant-client weaviate-client

# Phase 3: Graph RAG
pip install graphrag            # Microsoft GraphRAG
pip install lightrag-hku        # LightRAG
pip install neo4j               # Neo4j 드라이버
pip install neo4j-graphrag      # Neo4j GraphRAG

# Phase 4: 통합 + LLM
pip install langchain langchain-community langchain-chroma
pip install openai anthropic

# 시각화
pip install networkx pyvis plotly
```

### 프로젝트 구조 (예정)

```
poc/
├── EXPERIMENT_PLAN.md          ← 이 문서
├── requirements.txt
├── .env                        ← API 키 (git 제외)
├── data/
│   ├── raw/                    ← LLM 생성 원본 일기
│   ├── processed/              ← 메타데이터 포함 정제 데이터
│   └── ground_truth/           ← 평가용 정답 셋
├── src/
│   ├── data_generation/        ← 합성 데이터 생성 스크립트
│   ├── phase1_vector/          ← 순수 벡터 검색 실험
│   ├── phase2_hybrid/          ← 하이브리드 검색 실험
│   ├── phase3_graph/           ← Graph RAG 실험
│   ├── phase4_integrated/      ← 통합 파이프라인
│   └── evaluation/             ← 평가 프레임워크
├── notebooks/                  ← Jupyter 실험 노트북
│   ├── 01_data_exploration.ipynb
│   ├── 02_vector_search.ipynb
│   ├── 03_hybrid_search.ipynb
│   ├── 04_graph_rag.ipynb
│   └── 05_integrated_pipeline.ipynb
└── results/                    ← 실험 결과 저장
    ├── metrics/
    └── visualizations/
```

---

## 8. 실험 일정 (단계별)

| 단계 | 내용 | 산출물 |
|------|------|--------|
| **Step 0** | 합성 데이터 생성 + Ground Truth 구축 | `data/` 디렉토리 |
| **Step 1** | Phase 1 - 벡터 검색 실험 (모델/DB 비교) | 벤치마크 결과표 |
| **Step 2** | Phase 2 - 하이브리드 검색 실험 | Phase 1 대비 개선율 |
| **Step 3** | Phase 3 - Graph RAG 실험 | 관계 질문 답변 품질 |
| **Step 4** | Phase 4 - 통합 파이프라인 구축 | 최종 시스템 |
| **Step 5** | 전체 비교 분석 + 결과 정리 | 최종 보고서 |

---

## 9. 기대 결과 및 확인 사항

### 확인하고 싶은 핵심 가설

1. **벡터 검색만으로도 일기의 감정/주제 기반 검색은 충분히 유용하다**
2. **BM25 결합 시 이름/장소 같은 정확 매칭 성능이 크게 향상된다**
3. **Graph RAG는 관계 질문과 패턴 분석에서 압도적 우위를 보인다**
4. **LightRAG는 Microsoft GraphRAG 대비 비용 효율이 높으면서 80% 수준의 품질을 제공한다**
5. **통합 파이프라인의 쿼리 라우팅이 단일 기술 대비 전반적 성능을 향상시킨다**
6. **한국어 일기 데이터에서 BGE-M3가 MiniLM보다 유의미하게 좋은 성능을 보인다**

### 최종 산출물

- 기술별 벤치마크 비교표
- 쿼리 유형별 최적 기술 추천 가이드
- 통합 파이프라인 아키텍처 및 코드
- 실험 결과 시각화 대시보드
